server:
  port: 9000
realm_name: "master"
url_login_token: http://localhost:8080/realms/org-apae/protocol/openid-connect/token
client_id: client-apae
client_secret: LeqRThncBPvVAoxnqrLcTUeXY6kLHQ8X
issuer_uri: http://localhost:8080/realms/org-apae2

spring:
  application:
    name: auth
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: ${client_id}
            client-secret: ${client_secret}
            scope:
              - openid
        provider:
          keycloak:
              issuer-uri: ${issuer_uri}
      resourceserver:
        jwt:
          issuer-uri: ${issuer_uri}
